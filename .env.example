# Django Configuration Settings
SECRET_KEY=your-django-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Database Connection URL (e.g. postgres://user:pass@host:port/dbname)
# Leave blank in development to fallback to local SQLite database (db.sqlite3)
# Render automatically injects this in production when you link a database.
DATABASE_URL=
